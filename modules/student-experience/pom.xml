<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.6</version>
    <relativePath/>
  </parent>

  <groupId>com.campusos</groupId>
  <artifactId>student-portal</artifactId>
  <version>0.1.0</version>
  <name>CampusOS Student Experience Portal</name>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>17</maven.compiler.release>

    <!--
      SECURITY (A06, CWE-1395): these override the versions Spring Boot 3.5.6 manages.
      Remove an override only when the managed version has caught up past it.

      Thymeleaf 3.1.3.RELEASE (Boot-managed) is affected by three expression-sandbox bypasses:
      CVE-2026-40477 (9.1), CVE-2026-40478 (9.1) and CVE-2026-41901 (9.0), the last being a
      bypass of the fix for the first two — 3.1.4 is not sufficient, 3.1.5.RELEASE is. Every page in
      this app is a Thymeleaf template. No reachable path was found (probing confirms user
      input is rendered as data and never evaluated as an expression), so this is version
      hygiene rather than a demonstrated exploit here, but the engine is not one to run
      knowingly vulnerable.

      Tomcat 10.1.46 (Boot-managed) sits inside the 10.1.24-10.1.57 affected band; 10.1.55
      carries seven fixes. Most do not apply to this app — it uses no DIGEST auth, no AJP, no
      WebDAV and declares no servlet security constraints — so this is hygiene too.
    -->
    <thymeleaf.version>3.1.5.RELEASE</thymeleaf.version>
    <tomcat.version>10.1.55</tomcat.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.google.zxing</groupId>
      <artifactId>core</artifactId>
      <version>3.5.3</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
